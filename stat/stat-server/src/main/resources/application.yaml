server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ewm_stats_db
    username: postgres
    password: 54321
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true